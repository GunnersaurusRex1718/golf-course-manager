{
  "name": "golf-course-manager",
  "version": "0.1.0",
  "description": "A pixel art golf course management and creation simulator",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "gh-pages": "^6.0.0"
  },
  "dependencies": {
    "phaser": "^3.88.0"
  }
}
